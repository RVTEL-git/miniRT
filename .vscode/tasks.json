{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build: make",
      "type": "shell",
      "command": "make",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Lance le Makefile du projet (génère le binaire miniRT)."
    }
  ]
}